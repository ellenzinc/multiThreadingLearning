#include <pthread.h>

class Thread 
{
 public:
    Thread();
    virtual ~Thread();
    
    int start();
    int join();
    int detach();
    pthread_t self();
 private:
    pthread_t m_tid;
    int m_running;
    int m_detached;
};
